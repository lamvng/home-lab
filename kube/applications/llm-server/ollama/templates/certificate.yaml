apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "ollama.certificate" . }}
  namespace: {{ .Release.Namespace }}
spec:
  secretName: {{ include "ollama.certificate" . }}
  isCA: false
  usages:
    - server auth
  dnsNames:
    - "{{ .Values.domain }}"
  issuerRef:
    name: cert-manager-certmanager
    kind: ClusterIssuer
